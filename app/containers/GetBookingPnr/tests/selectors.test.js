// import { selectGetBookingPnrDomain } from '../selectors';

describe('selectGetBookingPnrDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
