// import { selectFlightDetailsViewDomain } from '../selectors';

describe('selectFlightDetailsViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
