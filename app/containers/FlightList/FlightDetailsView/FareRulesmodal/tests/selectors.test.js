// import { selectFareRulesmodalDomain } from '../selectors';

describe('selectFareRulesmodalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
