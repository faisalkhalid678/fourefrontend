// import { selectPaymentConfirmationDomain } from '../selectors';

describe('selectPaymentConfirmationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
